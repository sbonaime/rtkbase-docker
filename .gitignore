# Exported image archives produced by build.sh (large binaries, rebuildable on demand).
*.tar
*.tar.gz

# Runtime state generated by running the container locally (settings.conf, gnss data, logs).
# Contains real secrets (Flask session key, web login password hash) and potentially real
# GPS coordinates of the base station once configured -- never commit this.
rtkbase-data/
